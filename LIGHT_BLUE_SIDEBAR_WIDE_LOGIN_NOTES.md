# Light-Blue Sidebar and Wider Login Update

Presentation-only refinement:

- Replaced the deep navy sidebar with a brighter light-blue-to-blue gradient shared across every role workspace.
- Made sidebar navigation labels, sidebar headings, captions, and sidebar controls white and bold.
- Retained a darker blue only for the active navigation item, so the selected page remains obvious.
- Widened the centered login layout from a narrow column to a large, comfortable desktop card.

No workflows, role permissions, database tables, SQLite data, forms, or authentication logic were changed.
