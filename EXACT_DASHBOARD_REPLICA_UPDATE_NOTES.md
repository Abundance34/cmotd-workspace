# ProcureFlow Exact Dashboard Replica Update

This update applies the approved cobalt-blue enterprise command-centre visual design across every ProcureFlow role workspace.

## What changed

- Reference-style top command bar with visual search, notification, theme and user controls.
- Deep cobalt-blue sidebar with the CMOTD logo card, product lock-up, bold white navigation and selected state.
- Reference-style KPI card presentation, dashboards, forms, tables, action buttons, and empty-state containers.
- Profile card at the bottom of the sidebar.
- Existing login, roles, workflow routing, database queries, permissions, stored records and SQLite data remain untouched.

## Files changed

- `app.py`
- `core/ui.py`

No `data` files are included in the safe patch.
