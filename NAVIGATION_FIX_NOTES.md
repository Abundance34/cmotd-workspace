# In-Place Sidebar Navigation Fix

Sidebar navigation is now native in-place Streamlit controls for every role. This source package deliberately excludes the live `data` folder and local environment secrets.
