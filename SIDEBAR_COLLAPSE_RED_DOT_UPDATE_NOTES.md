# Sidebar Collapse + Red Attention Dot Update

This build adds a shared top-bar collapse/expand control for all signed-in roles, red task/notification dots, and a compact fitted CMOTD logo lock-up. Application logic and database content are not changed.
